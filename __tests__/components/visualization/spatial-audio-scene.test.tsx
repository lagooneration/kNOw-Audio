const { render } = require('@testing-library/react');
const SpatialAudioScene = require('../../../src/components/visualization/spatial-audio-scene');

test('hello world!', () => {
	render(<SpatialAudioScene />);
});